# ==========================================================
# HLI-01 Version 1.0.0
# Model Configuration
# ==========================================================

model:
  name: "BiLSTM_Attention"

  input_size: 63
  hidden_size: 128
  num_layers: 2
  num_classes: 4

  bidirectional: true
  use_attention: true

  dropout: 0.30